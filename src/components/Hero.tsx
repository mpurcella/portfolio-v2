// import Stars from '/videos/particles.mp4';

const Hero = () => {
    return (
        <section className="h-screen bg-black-400">
            {/* <video autoPlay muted loop className="h-full w-full opacity-30">
                <source src={Stars} type="video/mp4" />
            </video> */}
        </section>
    );
};

export default Hero;
